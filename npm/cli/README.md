# @koca-build/cli

Node wrapper for the `koca` CLI.

## Install

```bash
npm install -g @koca-build/cli
```

## Notes

- The installer currently downloads the Linux x64 release binary.
- Binary source: GitHub Releases for this repository.
