//! Interactive iocraft TUI handlers.
//!
//! Temporarily empty: the orchestration routes everything through the plain
//! handlers while this is rebuilt on iocraft (keeping the old look: front-loaded
//! gutter, blue spinner + bold headers, green/dim progress bars, grouped
//! confirm, and a full output dump on failure).
